document.addEventListener('click',event=>{const button=event.target.closest('[data-password-toggle]');if(!button)return;const id=button.getAttribute('data-password-toggle');const input=document.getElementById(id);if(!input)return;const showing=input.type==='text';input.type=showing?'password':'text';button.textContent=showing?'SHOW PASSWORD':'HIDE PASSWORD';button.setAttribute('aria-pressed',String(!showing));});
document.addEventListener('submit',event=>{const form=event.target;if(!(form instanceof HTMLFormElement)||form.action.endsWith('/api/register')===false)return;const a=form.querySelector('#password'),b=form.querySelector('#password_confirm');if(a&&b&&a.value!==b.value){event.preventDefault();b.setCustomValidity('Passwords do not match.');b.reportValidity();b.addEventListener('input',()=>b.setCustomValidity(''),{once:true});}});
