import type { RequestContext } from './types';import { ReportRepository } from './reportRepository';import { randomId,validSameOrigin } from './security';import { layout } from './views';
const html=(b:string,s=200)=>new Response(b,{status:s,headers:{'Content-Type':'text/html; charset=utf-8'}}),redirect=(to:string)=>new Response(null,{status:303,headers:{Location:to}}),esc=(s:unknown)=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
const allowed=['SPAM','HARASSMENT','THREAT','PERSONAL_INFORMATION','IMPERSONATION','MISLEADING_FALSE_CONTENT','OTHER'];function auth(c:RequestContext){return c.user?null:redirect('/login?return='+encodeURIComponent(c.url.pathname));}function repo(c:RequestContext){if(!c.db)throw new Error('V3 database unavailable');return new ReportRepository(c.db);}function back(c:RequestContext){const r=c.request.headers.get('Referer');if(!r)return '/';try{const u=new URL(r);return u.origin===c.env.APP_ORIGIN?u.pathname+u.search+u.hash:'/';}catch{return '/';}}
export async function reportPageV3(c:RequestContext,id:string){const no=auth(c);if(no)return no;return html(layout('Report comment',`<div class="eyebrow">COMMUNITY MODERATION</div><h1>REPORT COMMENT</h1><form class="panel" method="post" action="/api/comments/${esc(id)}/report"><div class="field"><label>REASON</label><select name="category">${allowed.map(x=>`<option>${x}</option>`).join('')}</select></div><button class="blue">SUBMIT REPORT</button></form>`,c.user));}
export async function reportCommentV3(c:RequestContext,id:string){const no=auth(c);if(no)return no;if(!validSameOrigin(c.request,c.env))return new Response('Invalid origin',{status:403});const fd=await c.request.formData(),category=String(fd.get('category')||'');if(!allowed.includes(category))return new Response('Invalid report',{status:400});const r=repo(c);if(await r.existing(id,c.user!.id,category))return new Response('You already reported this comment for that reason',{status:409});const now=Date.now();try{await r.insert(randomId(),id,c.user!.id,category,now);}catch{return new Response('Unable to submit report',{status:409});}const count=Number((await r.openCount(id,category))?.n||0);if(count>=10&&!await r.pending(id,category))await r.escalate({queueId:randomId(),commentId:id,category,count,now,auditId:randomId()});return redirect(back(c));}
