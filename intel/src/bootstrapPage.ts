import type { Env } from './types';

const css=`body{font-family:system-ui,-apple-system,sans-serif;background:#0b0b0c;color:#f5f5f5;margin:0}.wrap{max-width:620px;margin:auto;padding:32px 20px}.eyebrow{font-size:12px;letter-spacing:.16em;color:#aaa}h1{font-family:Georgia,serif;font-weight:500;font-size:34px;margin:10px 0}p{color:#bbb;line-height:1.55}.panel{border:1px solid #2b2b2e;background:#121214;padding:20px;margin-top:24px}label{display:block;font-size:12px;letter-spacing:.12em;margin-bottom:8px}input{box-sizing:border-box;width:100%;padding:14px;background:#09090a;border:1px solid #3b3b40;color:white;border-radius:0;font-size:16px}button{width:100%;margin-top:14px;padding:14px;border:0;background:#f1f1f1;color:#111;font-weight:700;font-size:14px}pre{white-space:pre-wrap;word-break:break-word;background:#080809;border:1px solid #29292c;padding:16px;min-height:80px;color:#d5d5d5}small{color:#888}`;
const escape=(s:string)=>s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]!));
const page=(mode:'validation'|'production',result='Ready.')=>{const production=mode==='production';return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>INTEL · ${production?'Production Migration':'Turso Validation'}</title><style>${css}</style></head><body><main class="wrap"><div class="eyebrow">INTEL V3 · ONE-TIME SYSTEM CONSOLE</div><h1>${production?'Production Turso migration':'Turso validation'}</h1><p>${production?'This applies any missing pinned INTEL Turso migrations and validates the production database. Existing migrations are skipped.':'This console is for the isolated validation database only.'}</p><form class="panel" method="post" action="${production?'/system/turso-production':'/system/turso-validation'}"><label for="token">BOOTSTRAP TOKEN</label><input id="token" name="token" type="password" minlength="32" required autocomplete="off" autocapitalize="none" spellcheck="false"><button type="submit">${production?'APPLY MISSING MIGRATIONS + VALIDATE':'MIGRATE + VALIDATE'}</button><p><small>The token is submitted directly to the INTEL Worker and is not stored by this page.</small></p><pre>${escape(result)}</pre></form></main></body></html>`;};
export function tursoBootstrapPage(env:Env,result?:string){if(!env.TURSO_VALIDATION_BOOTSTRAP_TOKEN||env.PERSISTENCE_PROVIDER==='turso')return new Response('Not found',{status:404});return new Response(page('validation',result),{headers:{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store','X-Robots-Tag':'noindex, nofollow'}});}
export function tursoProductionPage(env:Env,result?:string){if(!env.TURSO_VALIDATION_BOOTSTRAP_TOKEN||!env.TURSO_PRODUCTION_DATABASE_URL||!env.TURSO_PRODUCTION_AUTH_TOKEN)return new Response('Not found',{status:404});return new Response(page('production',result),{headers:{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store','X-Robots-Tag':'noindex, nofollow'}});}
