import type { RequestContext } from './types';import { EvidenceRepository } from './evidenceRepository';import { randomId,validSameOrigin } from './security';import { storeEvidence } from './evidence';const redirect=(to:string)=>new Response(null,{status:303,headers:{Location:to}});const STATES=['PRIMARY_SOURCE','PUBLIC_RECORD','VERIFIED','SECONDARY_SOURCE','UNVERIFIED','DISPUTED'];const MIME=new Set(['application/pdf','image/jpeg','image/png','image/webp','text/plain','text/csv','application/json']);function repo(c:RequestContext){if(!c.db)throw new Error('V3 database unavailable');return new EvidenceRepository(c.db);}
export async function uploadEvidence(c:RequestContext,investigationId:string){if(!c.user?.isAdmin)return new Response('Forbidden',{status:403});if(!validSameOrigin(c.request,c.env))return new Response('Invalid origin',{status:403});const fd=await c.request.formData(),file=fd.get('file'),title=String(fd.get('title')||'').trim(),description=String(fd.get('description')||'').trim(),state=String(fd.get('verification_state')||'UNVERIFIED');if(!(file instanceof File)||!title||title.length>300||description.length>10000||!STATES.includes(state)||!investigationId)return new Response('Invalid evidence',{status:400});if(file.size<1||file.size>20*1024*1024||!MIME.has(file.type))return new Response('Unsupported evidence file',{status:415});const evidenceId=randomId(),attachmentId=randomId(),now=Date.now(),safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,'_').slice(-120)||'evidence',key=`evidence/${investigationId}/${evidenceId}/${attachmentId}-${safeName}`;const stored=await storeEvidence(c.env,file,key);try{await repo(c).publish({evidenceId,investigationId,title,description,state,actor:c.user.id,attachmentId,key,filename:file.name.slice(0,255),mime:stored.mime,size:stored.size,sha256:stored.sha256,now,auditId:randomId()});}catch(e){await c.env.EVIDENCE.delete(key);throw e;}return redirect(`/evidence/${evidenceId}`);}
