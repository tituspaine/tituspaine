import {describe,expect,it} from 'vitest';import {checkRateLimit} from '../src/rateLimit';
describe('cheap abuse limiter',()=>{it('permits normal auth traffic and blocks a burst before database work',()=>{const r=new Request('https://intel.tituspaine.com/api/login',{headers:{'CF-Connecting-IP':'203.0.113.10'}});for(let i=0;i<12;i++)expect(checkRateLimit(r,'auth',1000)).toBeNull();const blocked=checkRateLimit(r,'auth',1000);expect(blocked?.status).toBe(429);expect(blocked?.headers.get('Retry-After')).toBeTruthy();});it('isolates rate classes',()=>{const r=new Request('https://intel.tituspaine.com/search',{headers:{'CF-Connecting-IP':'203.0.113.11'}});expect(checkRateLimit(r,'search',5000)).toBeNull();expect(checkRateLimit(r,'content',5000)).toBeNull();});});
